import { Icon } from "./Icons";
export default function EventEditor({ editing, form, setForm, onSubmit, onCancel, onBack }) {
  return <div className="view-stack narrow-view"><div className="view-heading"><div><button className="text-btn" onClick={onBack}><Icon name="back"/>Nazad</button><h1>{editing ? "Izmena događaja" : "Novi događaj"}</h1><p>{editing ? "Ažurirajte informacije o događaju." : "Popunite osnovne informacije i objavite događaj."}</p></div></div><section className="editor-card"><form className="modern-form" onSubmit={onSubmit}><div className="form-row full"><label>Naziv događaja<input value={form.title} onChange={e => setForm({...form,title:e.target.value})} placeholder="npr. Java konferencija" required/></label></div><div className="form-row full"><label>Opis<textarea value={form.description} onChange={e => setForm({...form,description:e.target.value})} placeholder="Kratak opis događaja..."/></label></div><div className="form-row two"><label>Lokacija<input value={form.location} onChange={e => setForm({...form,location:e.target.value})} placeholder="Beograd" required/></label><label>Kapacitet<input type="number" min="1" value={form.capacity} onChange={e => setForm({...form,capacity:e.target.value})} required/></label></div><div className="form-row two"><label>Početak<input type="datetime-local" value={form.startTime} onChange={e => setForm({...form,startTime:e.target.value})} required/></label><label>Kraj<input type="datetime-local" value={form.endTime} onChange={e => setForm({...form,endTime:e.target.value})} required/></label></div><div className="form-actions"><button type="button" className="secondary-btn" onClick={onCancel}>Otkaži</button><button className="primary-btn"><Icon name={editing ? "edit" : "plus"}/>{editing ? "Sačuvaj izmene" : "Kreiraj događaj"}</button></div></form></section></div>;
}
